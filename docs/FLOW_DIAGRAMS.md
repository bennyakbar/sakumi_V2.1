# Flow Diagrams (SVG/PNG)

Dokumen ini adalah pintasan ke versi visual flow yang bisa dilihat langsung.

- Buka indeks visual: [flows/README.md](/home/abusyauqi/sakumi/docs/flows/README.md)
- Format tersedia: `SVG` (utama), `PNG` (alternatif), dan `DOT` (sumber graphviz)

Untuk regenerate semua diagram:

```bash
python3 scripts/render_mermaid_flows.py
```
